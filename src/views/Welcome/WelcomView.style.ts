import {ThemedViewStyle} from '@styles';

export const styles: {
  container: ThemedViewStyle;
  topSection: ThemedViewStyle;
  bottomSection: ThemedViewStyle;
} = {
  container: {
    flex: 1,
    justifyContent: 'center',
  },
  topSection: {
    justifyContent: 'center',
  },
  bottomSection: {
    justifyContent: 'center',
  },
};
